#include "SelectionPolicy.h"

int MandatesSelectionPolicy::select() const{ //Selects the neighboring party with the most mandates.

}

int EdgeWeightSelectionPolicy::select() const{ // Selects the neighboring party with the highest edge
//weight.
    //TODO
}
